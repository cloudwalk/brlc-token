import { expect } from "chai";

// NOTE! The corresponding smart contract for which the tests from this file were used no longer exists.
// But this file is left for compatibility with existing E2E blockchain tests.
// After their modification, this file can be deleted.
describe("Contract 'BRLCTokenBridgeable'", async () => {
  it("Does not exist anymore", async () => {
    expect(0).to.equal(0);
  });
});
